server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # react-router의 클라이언트 사이드 라우팅 — /tasks/5 같은 경로를 새로고침해도
    # nginx가 404를 내지 않고 index.html을 돌려줘서 React Router가 처리하게 한다.
    location / {
        try_files $uri /index.html;
    }
}
